import React, { Component } from 'react';
import { connect } from 'react-redux';

interface __Props {}

interface __StateProps {}

class __ extends Component<__Props, __StateProps> {
	render() {
		return <>...</>;
	}
}

function mapStateToProps(state) {
	return {};
}

export default connect(mapStateToProps)(__);
